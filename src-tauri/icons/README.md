# Icons Directory

This directory should contain the application icons.

For Windows builds, `icon.ico` is required for the Windows Resource file.

To generate icons from a PNG:
1. Create a 1024x1024 PNG icon
2. Run: `pnpm tauri icon <path-to-icon.png>`

Or download the default Tauri icon set from the official examples.
